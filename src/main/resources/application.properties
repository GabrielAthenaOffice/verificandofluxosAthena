spring.application.name=fluxo
server.port=5000

# ============================================
# Database Configuration (PostgreSQL)
# ============================================
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/fluxosdb}
spring.datasource.username=${DATABASE_USER:postgres}
spring.datasource.password=${DATABASE_PASSWORD:Mn1@34567}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# ============================================
# Security Configuration (SHARED com ZapZap)
# ============================================
# IMPORTANTE: Usar o MESMO secret do ZapZap
api.security.token.secret=${JWT_SECRET:my-secret-key}

# ============================================
# Supabase Storage Configuration
# ============================================
supabase.url=${SUPABASE_URL:https://pwckwzrmqaxcqnjiupgh.supabase.co}
supabase.servicekey=${SUPABASE_SERVICEKEY:eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...}
supabase.bucket.name=${SUPABASE_BUCKET_NAME:fluxos-arquivos}
supabase.max.file.size=${SUPABASE_MAX_FILE_SIZE:104857600}

# ============================================
# File Upload Configuration
# ============================================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# ============================================
# Swagger/OpenAPI
# ============================================
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.api-docs.path=/v3/api-docs

# ============================================
# CORS Configuration
# ============================================
cors.allowed.origins=${CORS_ORIGINS:https://frontzapzapinterno.vercel.app/}

# ============================================
# Logging
# ============================================
logging.level.com.athena.fluxos=INFO
logging.level.org.springframework.security=DEBUG